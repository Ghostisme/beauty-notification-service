# 🚀 快速部署指南

## 现在你需要做的事情

### 📝 第一步：完成本地配置（在 Windows 上）

#### 1. 编辑抖音密钥
打开文件：[.env](.env)

修改这两行：
```
DOUYIN_APP_ID=你的真实AppID
DOUYIN_APP_SECRET=你的真实AppSecret
```

**如何获取？**
- 登录 https://developer.open-douyin.com/
- 进入「我的应用」
- 复制 App ID 和 App Secret

---

#### 2. 配置门店 Webhook
打开文件：[data/groups.json](data/groups.json)

**格式：**
```json
{
  "store_001": {
    "name": "朝阳店",
    "webhook": "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=真实的key",
    "region": "北京"
  }
}
```

**如何获取 Webhook？**
1. 企业微信群 → 群设置 → 群机器人 → 添加
2. 复制 Webhook 地址
3. 粘贴到 groups.json

**批量收集工具：**
用浏览器打开：[webhook-collector.html](webhook-collector.html)
- 边创建群边录入
- 最后导出 groups.json

---

### 🖥️ 第二步：上传到服务器

#### 方式A：使用 WinSCP（推荐）

```
1. 下载 WinSCP：https://winscp.net/
2. 连接服务器：
   - 主机：你的服务器IP
   - 用户名：root
   - 密码：你的密码
3. 上传整个 beauty-notification-service 文件夹到 /opt/
```

#### 方式B：使用 PowerShell

```powershell
# 1. 打包项目
cd "C:\Users\Administrator\Desktop\新建文件夹 (2)"
tar -czf beauty-notification.tar.gz beauty-notification-service

# 2. 上传（需要先安装 OpenSSH）
scp beauty-notification.tar.gz root@服务器IP:/opt/
```

---

### 🔧 第三步：在服务器上部署

#### SSH 登录服务器

```bash
# Windows PowerShell 或 CMD
ssh root@你的服务器IP
```

#### 解压并部署

```bash
# 1. 解压项目
cd /opt
tar -xzf beauty-notification.tar.gz
cd beauty-notification-service

# 2. 运行一键部署脚本
chmod +x deploy.sh
bash deploy.sh
```

**deploy.sh 会自动：**
- ✅ 安装 Docker 和 Docker Compose
- ✅ 配置防火墙（开放 3000、443 端口）
- ✅ 优化系统参数
- ✅ 配置日志轮转
- ✅ 创建监控脚本

#### 启动服务

```bash
# 启动
docker-compose up -d

# 查看日志
docker-compose logs -f
```

---

### ✅ 第四步：测试验证

#### 运行测试脚本

```bash
bash test.sh
```

**测试脚本会：**
1. 检查服务健康状态
2. 查看门店列表
3. 发送测试消息到第一个门店
4. 显示队列统计

#### 手动测试推送

```bash
curl -X POST http://localhost:3000/api/notify \
  -H "Content-Type: application/json" \
  -d '{
    "storeId": "store_001",
    "orderInfo": {
      "orderId": "TEST123",
      "customerName": "张女士",
      "phone": "138****8888",
      "service": "美甲服务",
      "amount": "99",
      "appointmentTime": "2026-09-17 14:00"
    }
  }'
```

**检查企微群是否收到消息！**

---

### 🔗 第五步：配置抖音回调

#### 在抖音开放平台配置

```
1. 登录 https://developer.open-douyin.com/
2. 进入你的应用
3. 找到「消息推送配置」
4. 填写回调地址：
   https://你的域名/api/douyin/callback
   
   或（如果没域名）：
   http://你的服务器IP:3000/api/douyin/callback
```

**⚠️ 注意：**
- 抖音可能要求 HTTPS
- 建议配置域名 + SSL 证书（Let's Encrypt 免费）

---

## 🎯 完整流程总结

```
1. 本地配置 .env 和 groups.json
   ↓
2. 上传到服务器 /opt/
   ↓
3. 服务器运行 deploy.sh
   ↓
4. 启动服务 docker-compose up -d
   ↓
5. 测试推送 bash test.sh
   ↓
6. 配置抖音回调地址
   ↓
7. 完成！等待真实订单测试
```

---

## 📊 监控命令

```bash
# 查看服务状态
docker ps

# 查看日志
docker-compose logs -f notification-service

# 查看资源占用
docker stats

# 查看队列统计
curl http://localhost:3000/api/queue/stats

# 健康检查
curl http://localhost:3000/health

# 一键监控（部署后可用）
beauty-monitor
```

---

## ❓ 常见问题

### Q1: 推送到企微群失败？
**检查：**
```bash
# 1. 查看日志
docker-compose logs -f

# 2. 手动测试 Webhook
curl -X POST "你的Webhook地址" \
  -H "Content-Type: application/json" \
  -d '{"msgtype":"text","text":{"content":"测试"}}'
```

### Q2: 服务启动失败？
```bash
# 查看详细错误
docker-compose logs

# 常见原因：
# - .env 文件格式错误
# - groups.json 格式错误
# - 端口 3000 被占用
```

### Q3: 如何更新配置？
```bash
# 1. 编辑配置文件
nano data/groups.json

# 2. 重启服务
docker-compose restart
```

### Q4: 如何添加新门店？
```bash
# 1. 编辑 groups.json，添加新门店
nano data/groups.json

# 2. 重启服务（自动加载新配置）
docker-compose restart notification-service
```

---

## 💰 总成本

| 项目 | 费用 | 周期 |
|------|------|------|
| 企业微信认证 | 300元 | 年付 |
| 云服务器 1核2G | 108-298元 | 年付 |
| 域名（可选） | 50元 | 年付 |
| **总计** | **458-648元/年** | - |

---

## 📞 需要帮助？

- [README.md](README.md) - 完整文档
- [IMPLEMENTATION.md](IMPLEMENTATION.md) - 实施指南
- 查看日志：`docker-compose logs -f`
- 测试推送：`bash test.sh`

---

**祝部署顺利！有问题随时问我。**
