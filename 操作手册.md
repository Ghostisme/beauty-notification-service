# 抖音来客订单 → 门店微信群 自动通知 · 操作手册

> 适用系统：抖音来客（服务商模式）订单通知
> 线上地址：**https://notification.hongquanquan.cn**
> 手册版本：v1.0（2026-09-20）

---

## 0. 一分钟看懂它在做什么

```
客户在抖音下单 / 留资
        │
        ▼
抖音来客开放平台  ──(服务器定时拉单, 只允许白名单 IP)──►  服务器 notification.hongquanquan.cn
        │                                                     │
        │                                        解析订单 → 解出手机号 → 生成通知文案
        │                                                     │
        │                                          写进【待发队列】(pending)
        │                                                     │
        │                                    中继(每 60 秒取一次)用微信号发出
        │                                                     │
        ▼                                                     ▼
   抖音来客后台                                    对应门店的微信群（群里有老板、店长、我们的运营）
```

**关键点三条：**

1. **全自动**：不用任何人点击确认，客户下单后 1 分钟内群里就能看到通知。
2. **客户不在群里**：客户是通过抖音来客留资/下单的，门店群是**内部接收点**（我们代运营方 + 美业企业老板 + 店长）。
3. **发消息用的是普通微信号**：所以不需要企业认证、可信域名、ICP 备案 —— 这也正是当初卡住的地方被绕开的原因。

---

## 1. 名词对照（免得看后台时发懵）

| 名词 | 意思 |
|---|---|
| **待发队列 / outbox** | 服务器上排队的通知，「待发」= 还没发出去 |
| **中继 / relay** | 拿着队列里的消息、用微信号真正发到群里的那个程序 |
| **门店群** | 一个门店一个微信群，群名形如 `品牌-省-市+门店`（示例：`玥笙-辽宁-大连中山店`） |
| **测试群** | 专门用来试链路的群（`抖音本地生活订单通知`），只进测试消息，不会碰门店群 |
| **商户** | 抖音来客里的一个账户（`account_id`），一个商户可以对应一个门店群 |
| **草稿/预览** | 只生成文案不发送，用来核对内容对不对 |

---

## 2. 日常只需要看三个地方

打开 **https://notification.hongquanquan.cn** → 输入访问令牌（令牌在服务器 `/opt/beauty-notification-service/.admin_token` 文件里）。

| 页签 | 什么时候看 | 什么算正常 |
|---|---|---|
| **订单数据** | 想知道今天进了哪些单 | 订单在不断增加、手机号有值 |
| **待发队列** | 怀疑通知没发出去 | 「待发」数字能自己归零；如果长时间不动 → 见第 6 节 |
| **运行日志** | 出问题时 | 有明确错误信息（含官方 logid，可直接拿去问抖音来客客服） |

> 判断「系统是不是活着」，看**待发队列**就够了：  
> 数字在涨 → 说明在拉单；数字又回到 0 → 说明中继发出去了。**一直停在同一个数字不动，就是中继断了。**

---

## 3. 一次性准备（只做一次，做完就不用管）

### 3.1 先试链路，再上真单

「待发队列」页签顶部有 **🧪 链路测试** 卡片：

1. 确认测试群名（默认 `抖音本地生活订单通知`）；
2. 点 **发一条测试消息**；
3. 等 1 分钟，去那个测试群看一眼。

**收到 = 整条链路通了**（服务器 → 微信号 → 微信群 → 回报）。没收到就别急着调门店群，先按第 6 节排查。
测试消息用完在待发队列里删掉即可（它只删自己那条，不会碰门店消息）。

### 3.2 把每个商户绑到它自己的门店群

「商户管理」→ 每个商户卡片里填 **微信接收群名称**：

| 商户（抖音来客账户） | 门店群名称（要和微信里显示的一字不差） |
|---|---|
| 超英皮肤定制管理 | 待填 |
| 长沙市尹欣美容咨询有限公司 | 待填 |
| 苏州一线美容有限公司 | 待填 |

> ⚠️ 群名必须**完全一致**：空格、括号（全角/半角）、emoji 都算。  
> 拿不准就在跑中继的那台机器上执行 `python wx_relay.py --list-groups`，把打印出来的名字原样复制过去。

### 3.3 让发消息的微信号进群

门店群是在企业微信里建的，要让**群主或群管理员把中继用的那个微信号拉进群**。  
建议用一个小号专门干这件事，别用主号。

### 3.4 中继跑在哪里

| 方式 | 说明 | 适合 |
|---|---|---|
| **服务器上的云端微信（推荐）** | 微信登录态保存在服务器容器里，页面登录一次即可，不用长期开电脑 | 长期稳定运行 |
| 某台 Windows 电脑 | 那台电脑要登录微信 PC 客户端并保持开机、不休眠 | 临时/备用 |

不管哪种，**同时只能有一个中继在跑**，两个一起跑会把同一条消息发两遍。

---

## 4. 常见日常操作

### 4.1 「某个群改名了，通知全断了」

这是最常见的故障。**改一个字段就能恢复，不要删队列、不要重建。**

1. 在中继那台机器上取新群名：`python wx_relay.py --list-groups`；
2. 后台 → 商户管理 → 找到该商户 → 把 **微信接收群名称** 改成新群名 → **保存**；
3. 保存那一刻，队列里所有还指着旧群名的消息（包括已经失败的）会**自动改指到新群名**并重新排队；
4. 提示里会写「已重绑定 N 条」，看到它就成了。

**不想靠自动的话**：「待发队列」页签顶部有 **🔗 群绑定** 卡片，一眼能看出「配置的群名 / 已对齐条数 / 残留的旧群名条数」，点「🔗 重新绑定」手工来一次。

> 判断标准：**「残留的旧群名」必须是 0**。不是 0 就说明还有消息发不出去。

### 4.2 改通知里显示的内容

「全局配置 → 展示字段」控制通知里显示哪些项：订单号 / 商品 / 金额 / 状态 / 客户 / 手机号 / 时间 / 门店。
想多显示就加上，不想要就去掉（逗号分隔）。**手机号默认是展示的**（群内都是自己人，需要它联系客户）。

### 4.3 一条消息发不出去（状态 failed）

待发队列里勾选 → **重发**。原因通常是：中继没在跑、群名对不上、微信号被限制。

### 4.4 想看通知长什么样，又不想真发

商户卡片 → **🧪 推送测试**（只打印文案，不进群）；或 **🔍 拉单预览**（拉真实订单、生成文案，但不发送、不写状态）。

---

## 5. 通知长什么样（示例）

```
🛍️【新订单提醒】
━━━━━━━━━━━━━━━━━━
🎉 您有一笔新的客户订单，请及时处理！
🧾 订单编号：xxxxx
👤 客户姓名：张女士
💰 订单金额：¥199
📦 订单内容：小气泡清洁
📱 手机号：138xxxxxxxx
🕒 下单时间：2026-09-20 12:00:00
━━━━━━━━━━━━━━━━━━
📲 请立即查看订单，并联系客户完成核销！
✅ 处理完成后，请及时更新订单状态。
```

> 群消息是**纯文本**（我把它从 Markdown 转成纯文本了），因为微信群不解析 Markdown；`@提醒` 在群里也不会生效。

---

## 6. 出问题怎么排查（对号入座）

| 症状 | 多半是这个原因 | 怎么处理 |
|---|---|---|
| 待发队列数字一直不动 | 中继没在跑 / 微信号掉线 | 看中继日志；云端容器重登微信（扫码） |
| 队列里出现 `failed` | 群名找不到、重名、或群被设成「不显示该聊天」 | 用 `--list-groups` 核对群名；把群从「不显示该聊天」恢复 |
| 提示「会话列表里找不到该群」 | 群改名了 / 中继微信号不在群里 / 群被隐藏 | 见 4.1；让群主把中继号拉进群 |
| 提示「两个群完全同名」 | 两个群重名，脚本宁可不发 | 在微信里把其中一个群改名，区分开 |
| 拉单报 `2119013 IP不在白名单` | 调用方 IP 不在来客白名单 | 只有服务器 IP `47.103.32.12` 在白名单，本机拉单必然失败；在服务器上操作 |
| 拉单报 `60020` | 企微可信域名/主体问题 | **不用管**：我们走的是微信号通道，不依赖这条 |
| 手机号是空的 | 该订单没有手机号，或解密失败 | 抖音来客后台确认该订单是否真有留资 |
| 通知重复发了两遍 | 有两个中继同时在跑 | 只保留一个（云端容器 **或** Windows，不要都开） |
| 后台打不开 / 401 | 令牌不对 | 令牌在服务器 `.admin_token` 文件里，注意别带空格 |

---

## 7. 部署与更新（本地提交 → 上传 → 云端 Docker）

**核心一句话**：代码在你**本机终端**提交、打包，再传到服务器解压、重建容器。
**服务器全程不需要连 GitHub** —— 国内直连 GitHub 经常几十 KB/s 甚至超时，这是这套流程存在的原因。

### 7.1 全流程一览

```
① 本机终端   改代码 → git commit → git push        （进仓库，备份+留痕）
② 本机终端   python tools/release.py               （打成一个几百 KB 的 tar.gz）
③ 本机终端   scp dist/dylk-*.tar.gz root@47.103.32.12:/tmp/
④ 服务器     bash tools/remote-deploy.sh /tmp/dylk-*.tar.gz
                └─ 备份 → 解压覆盖代码 → 补新配置 → docker compose up -d --build → 自检
⑤ 浏览器     https://notification.hongquanquan.cn   （令牌见服务器 .admin_token）
```

### 7.2 本机终端：提交代码

先 `cd` 到**有 `.git` 的那一层**（部署仓库目录）：

```bash
cd /d/github_code/余乐圈美业项目资料/beauty-notification-service

git status                                  # 看改了哪些文件
git add -A
git commit -m "feat: 一句话说明这次改了什么"
git push origin main                        # 推到 GitHub（备份+留痕；服务器不从这拉）
```

> 工作区 `dyLK-to-wecom` 本身不是 git 仓库，改完的源码要**先同步到 `beauty-notification-service`** 再提交。同步这一步我可以帮你做。

### 7.3 本机终端：打包

```bash
cd /d/github_code/余乐圈美业项目资料/beauty-notification-service

python tools/release.py              # 打包当前 HEAD
python tools/release.py v1.2.0       # 也可以指定 tag / commit
```

产物：`dist/dylk-<日期>-<时间>-<commit>.tar.gz`。

**包里只有 git 跟踪的代码文件** —— `config.json`、`.admin_token`、`data/`、`logs/`、`state/` 都不在包里，
所以解压到服务器**不会覆盖你的密钥、订单库和待发队列**。

### 7.4 上传 + 服务器部署

```bash
# 本机终端
scp dist/dylk-*.tar.gz root@47.103.32.12:/tmp/

# 服务器（ssh 登进去后）
cd /opt/beauty-notification-service
bash tools/remote-deploy.sh /tmp/dylk-20260920-170147-295eb7b.tar.gz
#                              ↑ 换成你实际上传的包名（release.py 会直接打印这行命令）
```

**不确定项目目录在哪？** 不用猜，后台容器自己会告诉我们：

```bash
docker inspect dylk-admin --format '{{ range .Mounts }}{{ .Source }}{{ "\n" }}{{ end }}'
# 输出里 ./config.json 所在的那一层就是项目目录（通常是 /opt/beauty-notification-service）
```

如果目录不是 `/opt/beauty-notification-service`，用 `APP_DIR=` 指定：

```bash
APP_DIR=/你的实际目录 bash tools/remote-deploy.sh /tmp/dylk-xxx.tar.gz
```

`remote-deploy.sh` 会自动做完四件事：

| 步骤 | 做了什么 |
|---|---|
| 1 | 备份 `config.json` / `data/admin.db` / `.admin_token` 到 `backups/`（带时间戳） |
| 2 | 解压发布包覆盖代码（密钥/数据/日志不在包里，不受影响） |
| 3 | 跑 `tools/patch-config.py` 补全新版本新增的配置项（**只补缺失的，不动已有值**） |
| 4 | `docker compose up -d --build` 重建容器 + 健康检查 |

> **只改配置、没动代码**时不用走这套：服务器上改完 `config.json` 后 `docker compose restart` 即可。

### 7.5 手动跑容器（不用部署脚本）

```bash
cd /opt/beauty-notification-service

ADMIN_TOKEN=$(cat .admin_token) docker compose up -d --build   # 重建并启动后台
docker compose restart                                          # 只重启（改完 config.json）
docker compose logs -f dylk-admin                               # 看后台日志
curl -s http://127.0.0.1:8787/api/health                        # 健康检查
```

### 7.6 配置升级补丁

新版本新增了「测试群」「隐私提醒开关」等配置项，服务器上的旧 config.json 里没有。在服务器上跑：

```bash
cd /opt/beauty-notification-service
cp config.json config.json.bak          # 保险起见先存一份

python3 tools/patch-config.py                     # 只补缺失项，不动任何已有值
python3 tools/patch-config.py --fix-target        # 顺便把推送通道切到 outbox
python3 tools/patch-config.py --drop-placeholder  # 顺便清掉 account_id 还是占位的空商户
```

它会顺带**体检并提醒**：目标群名没填的商户、`push.fields` 里没有 phone、占位空商户。重复跑无副作用。

### 7.7 备份与恢复

```bash
# 要备份的三样
config.json                # 所有密钥（remote-deploy.sh 每次自动备份）
data/admin.db              # 订单库 + 运行日志 + 待发队列
deploy/wechat/wxdata/      # 云端微信的登录态（丢了要重新扫码）
```

---

## 8. 中继（把消息真正发进群的那个程序）

### 8.1 它是什么、为什么必须有

企业微信**没有**给个人微信号开放任何发消息接口。所谓「用微信号自动发群」，本质是
**在一台机器上开着微信客户端 + 程序模拟人工操作**（搜索群名 → 打开会话 → 粘贴 → 回车）。

服务器只负责把通知写进**待发队列**；中继负责取出来、真正发到群里，再回报服务器标记「已发」。
所以：**中继不开 = 队列里的消息永远不动**（这就是第 2 节说的那个判断标准）。

### 8.2 跑法一：服务器上的云端微信（推荐，7×24 全自动）

登录态存在服务器容器里，不用长期开着某台电脑。

**① 启动容器**

```bash
cd /opt/beauty-notification-service/deploy/wechat

export ADMIN_TOKEN=$(cat ../../.admin_token)
export VNC_PASSWORD='自己设一个密码'        # 强烈建议设置

docker compose up -d --build
docker compose logs -f                      # 看启动过程（Ctrl+C 退出查看）
```

第一次构建要装微信客户端，耗时较久（几分钟到十几分钟），耐心等。

**② 扫码登录微信**（只需一次，之后容器重启也不用重扫）

最稳的方式是 SSH 隧道，不用改 nginx、不用暴露端口：

```bash
# 在本机终端执行（保持这个窗口开着）
ssh -L 6080:127.0.0.1:6080 root@47.103.32.12
```

然后本机浏览器打开 **http://127.0.0.1:6080/vnc.html** → 点连接（密码就是上面设的 `VNC_PASSWORD`）
→ 看到容器里的微信 → 用**小号**扫码登录。

> 想随时打开、不想每次开隧道时，也可以在宿主机 nginx 里把这个端口加 Basic Auth 反向代理出来。
> 但**一定要加密码** —— 6080 不加密码等于把微信画面公开在公网上。

**③ 把这个微信号拉进门店群**

门店群是企微群主建的，请群主或群管理员把**这个中继微信号**拉进对应门店群。
（这一步最容易漏，漏了就报「会话列表里找不到该群」。）

**④ 自检 + 看日志**

```bash
# 进容器做一次自检（环境 + 服务端连通性）
docker exec -it dylk-wechat python3 /opt/relay/wx_relay_linux.py \
  --server https://notification.hongquanquan.cn \
  --token $(cat /opt/beauty-notification-service/.admin_token) --check

# 中继日志（容器内 /data/relay.log，宿主机可这样看）
tail -f /opt/beauty-notification-service/deploy/wechat/wxdata/relay.log
docker compose logs -f
```

看到「✔ #N 已发送 → 群「xxx」」就是通了。
然后去后台「待发队列」页签点 **🧪 发一条测试消息**，到测试群里确认能收到。

**⑤ 常用容器命令**

```bash
cd /opt/beauty-notification-service/deploy/wechat
docker compose ps                  # 看容器状态
docker compose restart             # 重启（改了 wx_relay_linux.py 后执行这个就够，不用重建镜像）
docker compose down                # 停掉
docker compose up -d --build       # 升级镜像/重建
```

### 8.3 跑法二：你自己的 Windows 电脑（临时/备用）

不占用服务器资源，但**电脑必须开机、微信 PC 客户端保持登录**（可最小化，别锁屏休眠）。

```bash
pip install -U wxauto requests

# 先看本机微信里的真实群名（复制到后台用）
python wx_relay.py --list-groups

# 检查环境与令牌
python wx_relay.py --check

# 正式跑（常驻轮询）
python wx_relay.py --server https://notification.hongquanquan.cn --token 你的令牌

# 调试用：只跑一轮 / 只看不发
python wx_relay.py --server ... --token ... --once
python wx_relay.py --server ... --token ... --dry-run
```

### 8.4 ⚠️ 只能有一个中继在跑

云端容器和 Windows 电脑**同时开**会把同一条消息发两遍（客户会看到重复通知）。
换跑法时记得先把另一个停掉：`docker compose down` 或 Ctrl+C。

### 8.5 中继参数速查

| 参数 | 作用 | 默认 |
|---|---|---|
| `--server` | 服务地址 | `https://notification.hongquanquan.cn` |
| `--token` | 访问令牌（ADMIN_TOKEN） | 环境变量 `ADMIN_TOKEN` |
| `--interval` | 轮询间隔秒数 | 60（云端）/ 60 |
| `--limit` | 每轮最多发几条 | 3（云端）/ 5（Windows） |
| `--once` | 只跑一轮就退出 | — |
| `--dry-run` | 只打印不发送、不回报 | — |
| `--check` | 只检查环境与令牌 | — |
| `--list-groups` | 打印本机微信会话名（仅 Windows 版） | — |
| `--calibrate` | 打印窗口/屏幕信息并截图（仅云端版） | — |
| `--no-verify` | 关闭发送前的群名校验（**不推荐**） | — |

### 8.6 中继常见问题

| 症状 | 原因 | 处理 |
|---|---|---|
| 日志一直没有新行 | 队列是空的（正常） | 去后台发一条测试消息验证链路 |
| 「会话列表里找不到该群」 | 群改名了 / 中继号不在群里 / 群被设成「不显示该聊天」 | 见 4.1；让群主拉号进群；恢复显示 |
| 「两个群完全同名」 | 两个群重名，脚本宁可不发 | 在微信里改掉其中一个群名 |
| 微信掉线（容器里显示扫码页） | 登录态失效 | 重新开 SSH 隧道进 noVNC 扫码 |
| 发送失败 5 次变 failed | 见上面几条 | 后台待发队列勾选 → **重发** |
| 群里收到两条一样的 | 两个中继同时在跑 | 只留一个（见 8.4） |

---

## 9. 安全与风险须知（重要，请务必知道）

1. **访问令牌 = 后台钥匙**：能改配置、能触发推送，别发到群里、别截图外传。
2. **微信号自动化属于《微信个人帐号使用规范》之外的操作**，理论上存在账号被限制的可能。因此：
   - 用**小号**，不要用老板或客户经理的主号；
   - 保持**低频**（当前设置：每 60 秒取一次、每轮最多 3 条、每条间隔几秒）；
   - 只发订单通知这类**必要的服务消息**，不要拿来群发营销内容。
3. **手机号**：通知里带客户手机号，是给店内联系客户用的。目前群内都是自己人（运营 + 老板 + 店长），没有别的客户，所以不存在「被其他客户看到」的问题。
   将来如果要把同一条消息发进**有多个客户的群**，请先跟我说 —— 系统里有个开关可以按群名给这类群做提醒。
4. **密钥只存在服务器**：`config.json`（来客 client_secret、企微 secret）与 `.admin_token` 都不进代码仓库。

---

## 10. 后台页面字段对照表（哪个字段对应哪里）

> 页面：**https://notification.hongquanquan.cn** → 顶部分「**全局配置**」「**商户管理**」「**订单数据**」「**待发队列**」「**运行日志**」几个页签。
> 下面「配置文件里的位置」指的是服务器上的 `config.json`（`/opt/beauty-notification-service/config.json`）。

### 10.1 全局配置 → `config.json` 的 `wecom` / `douyin` / `push` / `sync`

| 页面上的字段 | 配置文件里的位置 | 作用 / 怎么填 |
|---|---|---|
| `client_key` | `douyin.client_key` | 抖音来客开放平台**服务商应用**的 App Key |
| `client_secret` | `douyin.client_secret` | 同一个应用的 Secret（手机号本地解密也用它） |
| 推送方式 | `wecom.target` | 只有 `outbox` 一种实际用法：服务器写待发队列，中继用微信号发到微信群 |
| 微信接收群名称 | `wecom.wechat_group` | 全局默认群名。**实际按商户各填各的**，这里是兜底默认值 |
| 增量拉单时间窗（分钟） | `sync.lookback_minutes` | 每次拉「最近 N 分钟内更新过的」订单，默认 60 |
| 手机号解密策略 | `douyin.phone_decrypt` | `local` 本地 AES 解密 / `api` 官方接口 / `off` 不取手机号 |
| 消息模式 per_order | `push.per_order` | `false` = 多单汇总成一条（推荐）/ `true` = 每单一条 |
| 展示字段（逗号分隔） | `push.fields` | 通知里显示哪些项。可选：`order_id`(订单号) `goods`(商品) `amount`(金额) `status`(状态) `customer`(客户) `phone`(手机号) `created_at`(下单时间) `shop`(门店) |
| 多客户群关键词 | `wecom.privacy_multi_customer_keywords` | 留空 = 不提醒（当前就是关的，因为门店群里只有自己人） |
| 〔折叠〕默认内部群 Webhook URL | `wecom.webhook_url` | **备用通道**，企微内部群专用，当前不用 |
| 〔折叠〕企微企业ID corpid | `wecom.corpid` | **备用通道**，企微官方接口用，当前不用 |
| 〔折叠〕企微应用 Secret | `wecom.secret` | **备用通道**，当前不用 |
| 〔折叠〕默认客户群 chat_id_list | `wecom.chat_id_list` | **备用通道**，当前不用 |
| 〔另〕敏感提醒要提示的字段 | `wecom.privacy_warn_fields` | 关键词命中时才生效，当前空数组 |
| 〔另〕**测试群** | `wecom.test_group` | 默认 `抖音本地生活订单通知`。「待发队列」页签的链路测试卡片默认读它 |

### 10.2 商户管理 → `config.json` 的 `merchants[]`（一个商户一项）

| 页面上的字段 | 配置文件里的位置 | 作用 / 怎么填 |
|---|---|---|
| 商户名称 | `merchants[].name` | 只是给你看的名字，出现在各处下拉里 |
| 来客商户根账户ID account_id | `merchants[].account_id` | **拉单的核心**，来自抖音来客的商户根账户 ID |
| 推送方式 | `merchants[].wecom.target` | 留空 = 继承全局；一般都留空，走 `outbox` |
| **微信接收群名称** | `merchants[].wecom.wechat_group` | **这个商户的通知发到哪个群**。必须与微信里显示的群名一字不差。**群改名了就改这里然后保存** |
| 〔折叠〕该商户群 Webhook URL | `merchants[].wecom.webhook_url` | 备用通道，当前不用 |
| 〔折叠〕企微 corpid / Secret / userid sender | `merchants[].wecom.*` | 备用通道，当前不用 |
| 〔折叠〕客户群 chat_id_list | `merchants[].wecom.chat_id_list` | 备用通道，当前不用 |
| 〔另〕该商户的通知显示字段/模板 | `merchants[].push.*` | 想给某个商户单独定制时写，留空则继承全局 `push` |

### 10.3 待发队列页签

| 页面元素 | 后端接口 | 作用 |
|---|---|---|
| **🧪 链路测试** 卡片的「测试群名称」输入框 | 入队时写进队列行的 `target` | 临时指定测试群；留空则用 `wecom.test_group` |
| 「🧪 发一条测试消息」按钮 | `POST /api/outbox/test` | 往测试群塞一条带时间戳的测试消息，验证「队列 → 中继 → 群 → 回报」整条链路 |
| **🔗 群绑定** 表格「配置的群名」 | 该商户的 `wecom.wechat_group` | 后台认为该商户应该发到哪个群 |
| 「已对齐（待发）」 | 队列里 `target` = 配置群名的条数 | 这些能正常发出去 |
| 「残留的旧群名（待发/失败）」 | 队列里 `target` ≠ 配置群名的条数 | **这个数字必须是 0**，不是 0 说明有消息发不出去 |
| 「🔗 重新绑定」/「全部重新绑定」 | `POST /api/outbox/rebind` | 把残留消息改指到当前配置的群名，failed 的重置为待发 |
| 「🔄 检查」 | `GET /api/group/binding` | 重新拉一次上面的体检数据 |
| 队列表格「目标群」 | 队列行的 `target` | 这条消息入队时写死的群名 |
| 「状态」 | `pending`(待发) / `sent`(已发) / `failed`(失败 5 次后) | 判断中继有没有在干活 |
| 「↻ 重发所选」/「🗑 删除所选」 | `POST /api/outbox/retry` / `delete` | 手动干预单条消息 |

### 10.4 商户卡片上的按钮 → 后端接口

| 按钮 | 后端接口 | 作用 |
|---|---|---|
| 🔍 拉单预览 | `GET /api/orders/preview` | 真的去抖音来客拉订单并生成文案，但**不发送、不写去重状态** |
| 📤 只入队（零备案） | `POST /api/outbox/enqueue` | 只把文案写进待发队列，等中继发 |
| 🧪 推送测试 | `POST /api/push`（test 模式） | 只打印文案，什么都不发 |
| 🚀 推送 | `POST /api/push` | 走完整流程（去重 → 生成 → 入队/发送） |
| 👥 查客户群 | `GET /api/wecom/groups` | 备用通道：列企微客户群 chat_id |

---

## 11. 一句话总结

> 客户在抖音下单 → 服务器拉单生成文案 → 微信号自动发到对应门店群。  
> 你要做的只有两件：**群改名了就去后台改「微信接收群名称」并按保存**；**收不到通知就先看「待发队列」和测试群**。
