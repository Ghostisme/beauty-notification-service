# 企业微信消息推送配置指南

## 第一步:获取企业ID

1. 打开浏览器,访问: https://work.weixin.qq.com/
2. 用管理员账号登录
3. 页面最下方,点击 **"我的企业"**
4. 找到 **"企业信息"** → 复制 **"企业ID"**

```
示例: ww1234567890abcdef
```

---

## 第二步:创建自建应用

### 2.1 创建应用

1. 左侧菜单: **应用管理** → **应用** → **自建**
2. 点击 **"创建应用"**
3. 填写信息:
   - **应用名称**: `订单通知助手`
   - **应用介绍**: `用于自动推送订单消息到客户群`
   - **应用logo**: 可以上传一个图标(选填)
   - **可见范围**: 选择所有需要的部门/成员
4. 点击 **"创建应用"**

### 2.2 获取应用凭证

创建后会自动跳转到应用详情页:

1. 找到 **AgentId**
   ```
   示例: 1000002
   ```

2. 找到 **Secret** → 点击 **"查看"**
   ```
   示例: abcdefghijklmnopqrstuvwxyz123456789
   ```
   
   ⚠️ **重要**: Secret只显示一次,记得复制保存!

### 2.3 设置应用权限

在应用详情页往下滚动:

1. 找到 **"企业客户权限"**
2. 勾选:
   - ✅ 客户联系
   - ✅ 客户群
   - ✅ 发送消息到企业的客户群
3. 点击 **"保存"**

---

## 第三步:创建发消息的员工账号

1. 左侧菜单: **通讯录**
2. 选择一个部门(比如"客服部")
3. 点击 **"添加成员"**
4. 填写信息:
   - **姓名**: `订单小助手` (客户会看到这个名字)
   - **账号(UserID)**: `OrderAssistant` (英文,代码里要用)
   - **手机号**: 填一个接收验证码的手机号
   - **部门**: 客服部
5. 点击 **"保存"**

### 3.1 把这个员工加入客户群

⚠️ **关键步骤**:

1. 在企业微信APP登录这个新账号(`订单小助手`)
2. 把这个账号加入所有需要推送消息的客户群
   - 可以让管理员把它拉进群
   - 或者创建新群时就把它加进去

---

## 第四步:填写配置文件

把上面获取的信息填入 `.env` 文件:

```env
# 企业信息
WEWORK_CORP_ID=ww1234567890abcdef              # 第一步获取的企业ID

# 应用信息
WEWORK_AGENT_ID=1000002                        # 第二步获取的 AgentId
WEWORK_SECRET=abcdefghijklmnopqrstuvwxyz...   # 第二步获取的 Secret

# 发送者信息
SENDER_USERID=OrderAssistant                   # 第三步创建的员工账号
```

### 示例(填好的样子):

```env
WEWORK_CORP_ID=ww7a8b9c0d1e2f3456
WEWORK_AGENT_ID=1000002
WEWORK_SECRET=abc123def456ghi789jkl012mno345pqr678stu901
SENDER_USERID=OrderAssistant
```

---

## 第五步:测试

### 5.1 启动服务

```bash
cd "C:\Users\Administrator\Desktop\新建文件夹 (2)\beauty-notification-service"
npm install
node wework-employee-sender.js
```

### 5.2 同步群列表

打开新终端,执行:

```bash
curl -X POST http://localhost:3000/groups/sync
```

应该能看到:

```json
{
  "success": true,
  "message": "已同步 X 个群",
  "groups": [...]
}
```

### 5.3 发送测试消息

```bash
curl -X POST http://localhost:3000/test/send -H "Content-Type: application/json" -d "{\"message\":\"🔔 测试通知\n这是订单小助手的第一条消息!\"}"
```

然后去企业微信客户群看,应该能收到消息!

---

## 常见问题

### Q1: 找不到"企业ID"?

确保你是**管理员身份**登录,普通成员看不到企业信息。

### Q2: Secret 显示不了?

只有**管理员**或**应用创建者**能查看 Secret。

### Q3: 消息发不出去?

检查:
1. ✅ 员工账号 `OrderAssistant` 是否已加入目标客户群
2. ✅ 应用权限是否开启了"客户联系"
3. ✅ `.env` 配置是否正确

### Q4: 提示"不合法的agentid"?

AgentId 填错了,去应用详情页重新复制。

---

## 配置检查清单

完成配置后,确认:

- [ ] 已获取企业ID
- [ ] 已创建自建应用
- [ ] 已获取 AgentId 和 Secret
- [ ] 应用已开启"客户联系"权限
- [ ] 已创建员工账号 `OrderAssistant`
- [ ] 该员工已加入所有客户群
- [ ] `.env` 文件已正确填写
- [ ] 测试消息发送成功

全部打勾后,就可以对接抖音消息了!
